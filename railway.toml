[build]
builder = "nixpacks"

[deploy]
# Arranca el backend; uvicorn lee $PORT que inyecta Railway.
startCommand = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
